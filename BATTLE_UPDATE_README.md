# 몽글 블록 온라인 v2 — 공격 대전 업데이트

이 ZIP은 이미 온라인 v1이 적용된 프로젝트에 덮어쓰는 업데이트입니다.
기존 서버 설정, PDF 기능, 혼자하기 파일은 포함하지 않습니다.
대전 서버를 새로 만들거나 추가 환경변수를 설정할 필요는 없습니다.
현재 mongle-game-server와 프론트 서비스를 함께 업데이트하세요.

## 적용

1. ZIP을 풀어 frontend와 game-server 내용을 C:\project\pdf_page_editor_render_split의 같은 폴더에 합칩니다.
2. 폴더를 삭제하거나 교체하지 말고 해당 파일만 덮어씁니다.
3. 기존 frontend/block-online-config.js의 대전 서버 주소는 그대로 유지합니다.
4. 아래 명령으로 테스트하고 커밋·푸시합니다.

~~~powershell
cd C:\project\pdf_page_editor_render_split
npm test --prefix game-server
git add frontend/block-battle-core.js frontend/block-online-engine.js frontend/block-online.html frontend/block-online.css frontend/block-online.js game-server/rooms.cjs game-server/server.cjs game-server/test/battle.test.cjs BATTLE_UPDATE_README.md
git diff --cached --stat
git commit -m "온라인 블록 공격 대전과 T스핀 방해 줄 구현"
git push
~~~

Render에서 프론트와 mongle-game-server 둘 다 새 커밋으로 배포됐는지 확인하세요.
자동 배포가 꺼져 있다면 해당 서비스의 수동 배포 기능으로 최신 커밋을 배포하세요.
게임 서버가 재시작하면 진행 중이던 방은 종료되므로 경기 중인 사람이 없는 때 적용하는 것이 좋습니다.
배포 후 Ctrl + Shift + R로 새로고침하고 새 방을 만드세요.
새 프론트가 구 서버에 연결되면 서버 업데이트 안내가 표시됩니다.

## 두 가지 모드

방 만들기에서 선택합니다. 모드는 방별로 고정되고 재대결에서도 유지합니다.
다른 모드를 하려면 새 방을 만드세요.

| 모드 | 규칙 |
| --- | --- |
| 3분 점수전 | 기존 방식 그대로. 공격 없이 180초 동안 점수 경쟁 |
| 공격 대전 | 시간 제한 없이 마지막 생존자가 승리. 먼저 탈락하면 관전 |

공격 대전 순위는 생존 여부와 탈락 시각 순서입니다. 점수나 공격량 순위가 아닙니다.
아직 살아 있는 사람들은 잠정 공동 1위로 표시됩니다.
같은 서버 판정 시점에 탈락하면 공동 순위이며, 마지막 참가자들이 동시에 탈락하면 무승부입니다.
연결 끊김은 기존처럼 20초 복귀 유예 후 탈락 처리합니다. 직접 나가면 즉시 탈락합니다.

## 공격량

| 행동 | 생성하는 공격 |
| --- | ---: |
| 일반 1줄 | 0줄 |
| 일반 2줄 | 1줄 |
| 일반 3줄 | 2줄 |
| 일반 4줄 | 4줄 |
| T스핀 미니, 제거 없음 | 0줄 |
| T스핀 미니, 1줄 제거 | 1줄 |
| T스핀 싱글 | 2줄 |
| T스핀 더블 | 4줄 |
| T스핀 트리플 | 6줄 |
| 퍼펙트 클리어 | 총 10줄 |

퍼펙트 클리어는 블록 고정과 줄 제거 후 보드가 완전히 비었을 때입니다.
기본 공격과 더하지 않고 총 10줄로 대체합니다.
콤보와 백투백은 별도 공격 보너스를 주지 않습니다. 기존 점수 계산은 유지합니다.

## 공격·상쇄·방해 줄

1. 내 블록이 고정되면 서버가 제거한 줄과 T스핀·퍼펙트 클리어 여부를 계산합니다.
2. 생성한 공격은 내가 받을 대기 줄부터 상쇄합니다. 예: 공격 4줄, 대기 3줄이면 상쇄 3줄 후 1줄을 보냅니다.
3. 남은 공격은 살아 있는 상대 한 명에게 전송합니다. 상대를 차례로 순환하며 탈락자는 건너뜁니다.
4. 내 보드 위에 다음 공격 대상이 표시되고 해당 상대 보드의 테두리가 강조됩니다.
5. 도착한 방해 줄은 1초 동안 대기합니다. 그 뒤 내가 블록을 고정할 때 최대 8줄씩 아래에서 올라옵니다.
6. 나머지 줄은 대기열에 남습니다. 대기열은 최대 60줄이며 초과 전송분은 버립니다.
7. 각 공격 묶음은 같은 위치에 구멍이 하나 있는 회색 줄입니다. 구멍 위치는 서버가 결정합니다.
8. 쌓인 블록이 밀려 넘치거나 새 블록 위치가 막히면 탈락합니다.

대기 줄 수, 실제 보낸 줄 수, 상쇄한 줄 수, 최근 공격 판정을 표시합니다.
상대 순위표의 공격량은 참고 정보이고 승패는 생존으로 결정됩니다.
블록 순서에 쓰는 난수와 방해 줄 구멍 난수는 분리해 같은 블록 순서 조건을 유지합니다.

## T스핀과 회전

공격 모드에만 방향별 벽·바닥 킥과 T스핀 판정을 적용합니다.
혼자하기 및 기존 3분 점수전의 회전 방식은 변경하지 않습니다.

- T 블록을 마지막으로 성공적으로 회전시켰고 중심 주변 대각선 네 칸 중 세 칸 이상이 막혀 있을 때 판정합니다.
- T가 향한 쪽의 두 모서리가 막혀 있거나 마지막 킥 후보로 회전한 경우 일반 T스핀입니다.
- 나머지는 미니이며 2줄 이상 제거하는 회전은 일반 T스핀으로 분류합니다.
- 성공한 좌우 이동이나 아래 이동 후에는 회전 판정을 해제합니다. 실패한 입력은 직전 유효 동작을 바꾸지 않습니다.
- 하드 드롭으로 실제로 내려간 경우 회전 판정을 해제하고, 이미 바닥에 있어 이동 거리가 0이면 유지합니다.
- 벽과 바닥도 막힌 모서리로 취급합니다.

특정 상용 게임의 모든 판정·점수 규칙을 복제한 것은 아닙니다. 이 업데이트에서는 위 규칙을 고정해 사용합니다.

## 파일 구성

- frontend/block-battle-core.js: 공격 전용 회전·T스핀·공격량·방해 줄 엔진
- frontend/block-online-engine.js: 공격 모드 상태 복원 및 입력 예측
- frontend/block-online.html / .css / .js: 모드 선택, 대상·대기 줄·공격 결과 UI
- game-server/rooms.cjs: 방 모드, 공격 상쇄/전송, 생존 판정
- game-server/server.cjs: 신규 파일 제공, 모드 요청과 호환성 확인
- game-server/test/battle.test.cjs: 공격 대전 회귀 검사

추가 npm 패키지는 없습니다. 서버 Build/Start Command와 ALLOWED_ORIGINS 설정도 그대로 사용합니다.
배포와 원격 Git push는 이 파일 제작 과정에서 실행하지 않았습니다.

## 검증

- npm test: 총 13개 검사 통과. 기존 점수전·혼자하기 검사 포함.
- 실제 회전으로 T스핀 미니·싱글·더블·트리플 슬롯에 들어가 고정하는 판정을 검사했습니다.
- 공격량, 퍼펙트 클리어 10줄 대체, 상쇄, 순환 대상, 1초 대기/고정 시 방해 줄 상승, 넘침 탈락을 검사했습니다.
- 공격전의 3분 이후 계속 진행, 생존 순위, 동시 탈락 무승부, 재대결 초기화를 검사했습니다.
- 실제 WebSocket 서버 + DOM 화면 4개로 모드 선택, 공격 표시, 회색 줄 상승, 대상 표시, 재접속, 생존자 결과, 재대결을 검사했습니다.
- 별도 점수전 화면 연동 검사도 통과했습니다.
- 실제 Canvas 렌더링에서 회색 방해 줄을 확인했습니다.
- 실제 브라우저 전체 CSS 배치, 모바일 실기기, 운영 서버 인터넷 지연에서의 플레이는 미검증입니다.

배포 후 PC 2대에서 점수전과 공격전을 각각 한 판씩 진행해 확인하세요.
